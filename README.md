Steps to run android 
1. before run buid need to start server 
`npx json-server db.json`

3. change url address in `api.ts` file const baseUrl variable
eg. http://192.168.1.11:3000

5. run command in terminal 
`npm run android`
